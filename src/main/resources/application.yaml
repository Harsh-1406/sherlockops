spring:
  application:
    name: opssleuth

  datasource:
    url: jdbc:postgresql://localhost:5432/sherlockops
    username: opssleuth
    password: opssleuth

  jpa:
    hibernate:
      ddl-auto: validate
      open-in-view: false

  flyway:
    enabled: true